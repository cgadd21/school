Cooper Gadd

1. 
  Folder is a component, File is a composite, Text and Sound are both a leaf. Folder is the abstract, File(s) can be either Text or Sound (in this example).

2.
  Drawable is a component, Group is a composite, Rectangle and Ellipse are both a leaf. Drawable is the abstract, Group which is either Rectangle or Ellipse.

3.
  Build is a component, Structure is a composite, Brick is leaf. Build is the abstract, Structure contains how to build legos, and Brick is used to build.
